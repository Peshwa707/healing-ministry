{
  "name": "healing-ministry",
  "version": "1.0.0",
  "description": "Islamic Ruqyah & Spiritual Healing App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
